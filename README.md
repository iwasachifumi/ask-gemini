# ask-gemini (AI-Driven Development Orchestrator)

> ⚠️ **本ツールは個人検証中の実験的スクリプトです。** 環境変数の設定・APIキーの管理・ローカルサーバーの起動といった技術的操作に慣れた方を対象としています。初心者向けではありません。動作・品質は無保証であり、利用は自己責任でお願いします。フィードバック歓迎です。

**ask-gemini** は、中規模コードベースにおける AI 駆動開発（AIDD）の「トークン上限超過・ハルシネーション・APIコスト」といった問題への対処を試みる、独立型の Node.js スクリプト（プロキシ）です。

200万トークンのコンテキストを無料枠で扱える **Gemini 1.5 Pro (Google)** と **Claude Code (Anthropic)** の間を取り持ち、役割を分担させることを狙っています。Geminiのマルチモーダル能力を使い、画像やPDFをコンテキストに含めることもできます（動作は環境・ファイルサイズによって変わります）。

## 💡 本ツールが解決する課題（Information Diet & Multi-modal）

大規模なプロジェクトを直接 Claude Code 等のエージェントに読ませようとすると、一瞬でコンテキストウィンドウを食いつぶし破綻します。

`ask-gemini` はこの問題を以下の**情報ダイエット（Information Diet）**と最適化アプローチで解決します：

1. **完全なステートレス・全ソース送信**: `git ls-files` と連動し、画像や巨大なロックファイル等のバイナリを除外した「人間が管理している純粋なテキストソース」のみ（約1.5MB以内）に絞り込みます。
2. **Gemini による俯瞰と執刀指示**: 絞り込んだ最新の全ソースを Gemini 1.5 Pro に一括送信し、全体構造を把握した上での「具体的な手術計画書（どのファイルをどう直すか）」を生成させます。画像（スクショなど）が指定された場合は、Base64エンコードして同時に送信し、視覚的な崩れもコードと照らし合わせて分析させます（※10MB以上の巨大ファイルは自動スキップ）。
3. **Claude への受け渡し**: Claude Code は、出力された「手術計画書」だけを読み取り、数個のファイルへの直接編集を行います。

## 🛡️ フェイルセーフ（エラー時の自律リカバリ機能）

Geminiのレート制限（HTTP 429 等）やネットワークエラーに達した場合、パニックを起こしません。
代わりに、**Claude Code が解釈して自律実行できる特別な「フォールバックメッセージ」を出力して正常終了 (exit 0)** します。これにより、Claude は即座に「自分の力だけでタスクを完了する」自律解決モードへ移行します。

## ⚙️ セットアップ

### 1. ファイルの配置

`ask-gemini.mjs` と `Claude_ask-gemini.md` を、対象の Git リポジトリの**ルートディレクトリ**にコピーします。

```
your-project/
├── ask-gemini.mjs       ← ここに置く
├── Claude_ask-gemini.md ← ここに置く
└── ... (既存のファイル)
```

### 2. 初回起動（自動セットアップ）

初回起動時に、以下の内容を対話形式で確認・実行します。

```
[初回セットアップ] 以下の処理を行います：
  1. Gemini API キーを ~/.ask-geminirc.json に保存 (パーミッション 0o600)
  2. ask-gemini.mjs と .ask-gemini-log.json を .git/info/exclude に追記
     （これらのファイルをプロジェクトの git 管理対象から除外します）

  claude.md への統合（任意）:
  3. Claude_ask-gemini.md の内容を claude.md に追記します
     ※ claude.md が存在する場合は末尾に追記、存在しない場合は新規作成

続行しますか？ [Y/n]:
```

処理 1〜2 はツールの動作に必要なセットアップです。処理 3 は任意で、Claude Code との `@gemini` ショートカット連携を有効にします。

> **Note**: `.git/info/exclude` はローカル専用の除外設定です。プロジェクトの `.gitignore` は一切変更しません。

### キー管理

* **依存関係ゼロ（Zero-Dependency）**: `fs`, `child_process`, `https(fetch)` のみを使用。
* **セキュアなキー保存**: Gemini API キーは `~/.ask-geminirc.json` にパーミッション `0o600` で保存されます。
* **キーの再設定**: `node ask-gemini.mjs --reset-key` で既存のキーを削除・再設定できます。

## 🚀 使い方

`ask-gemini.mjs` を対象の Git リポジトリのルートディレクトリに配置して実行します。

### 一般的な使用方法

```
node ask-gemini.mjs "カレンダーコンポーネントの表示崩れを治すための手術計画書を作成して"
```

### コマンドラインオプション

```
node ask-gemini.mjs --help          # ヘルプメニューの表示
node ask-gemini.mjs --reset-key     # 保存されたAPIキーの再設定
node ask-gemini.mjs "この画像のズレを直して" --attach "./error.png" # マルチモーダル（画像/PDF送信）
```

---

## 🤖 Claude Code（claude.md）との連携

`Claude_ask-gemini.md` の内容を Claude Code の指示ファイル（`claude.md`）に追記することで、`@gemini` ショートカットが有効になります。

**追記方法**（初回セットアップ時の対話で自動追記することも可能です）:

```bash
cat Claude_ask-gemini.md >> claude.md
```

追記後は、以下のように `@gemini` で始めるだけで Gemini への分析依頼が完結します：

```
@gemini カレンダーコンポーネントの表示崩れを治して
@gemini このスクショのようにレイアウト崩れてるの直して  # スクリーンショットを添付した場合
```

`Claude_ask-gemini.md` の内容には以下が含まれています：

- `@gemini` ショートカットの定義と画像転送ルール
- plan 受け取り後の必須チェック項目（不正な計画書の実行を防止）
- 実行中の Fallback Protocol（エラー時の即時停止・報告フォーマット）

---

*本ツールは個人開発・中規模リポジトリ（〜5万行程度）での利用を想定した実験的なスクリプトです。設計思想の根幹は「Gemini俯瞰 → 計画書生成 → Human-in-the-Loop検証 → Claude精密実行」の分業と、固定費最適化（Claude Pro/Max + Gemini無料枠）にあります。`ANTHROPIC_BASE_URL` を切り替えることで Claude Code のバックエンドをローカルLLMに向けることも可能ですが、本ツールのスコープ外です（興味があれば各自で検証を）。不具合・改善提案はIssueやFBでお知らせください。*
